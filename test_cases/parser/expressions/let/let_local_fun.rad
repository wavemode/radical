x = let
        local fun add(a: Int, b: Int) -> Int =
            a + b
    in
        add(2, 3)

(*
FAIL("Local declarations are not allowed in let expressions at test_cases/parser/expressions/let/let_local_fun.rad:2:9")
*)
