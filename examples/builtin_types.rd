a: int = 5

b: number = 5.0

c: string = "five"

d: bool = true

// list
e: [int] = [1, 2, 3, 4, 5]

// tuple
f: (int, number, string) = (10, 20.0, "thirty")

// dictionary / map
g: {string, int} = {
    "one": 1,
    "two": 2,
    "three": 3,
}

// set
h: {int} = {1, 2, 3, 4, 5, 4, 3, 2, 1}  // duplicates will be ignored

i: null = null

f: any = "can be any value"
